display_information:
  name: AFK bot
  description: Tracks AFK status on a shared channel Canvas
  background_color: "#2c2d30"
features:
  bot_user:
    display_name: "AFK bot"
    always_online: true
  slash_commands:
    - command: /afk
      description: "AFK bot: /afk <number> [comment] | back | lang ru|en|pl|uk|be|es | wait @user"
      usage_hint: "N [comment] | back | lang [code] | wait @user"
      should_escape: true
  interactivity:
    is_enabled: true
oauth_config:
  scopes:
    bot:
      - commands
      - canvases:write
      - chat:write
      - users:read
settings:
  socket_mode_enabled: true
  token_rotation_enabled: false
